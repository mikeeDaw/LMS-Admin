// import { utapi } from "../server/uploadthing";

// async function uploadFiles(formData: FormData) {
//   const filers = formData.getAll("files");
//   const response = await utapi.uploadFiles(filers);
//   //    ^? UploadedFileResponse[]
// }
